# Directories:
*client* is (currently) for React, assignment1

*server* is for a go backend server, assignment 2. Currently extremely barebone and unusable.

*static-pages* currently ignored after a bombshell was dropped
