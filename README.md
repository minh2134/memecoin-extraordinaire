# Directories:
*client* is (currently) for React, probably move to either a simple flask/django server for frontend (or expressJS with Nunjucks). Probs safely ignored.

*server* is for a go backend server, assignment 2. Currently extremely barebone and unusable.

*static-pages* currently is for holding all htmls until we made it clear what we need to submit in assignment 1
