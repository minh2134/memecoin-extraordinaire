# set Sepolia testnet node
ME_SEPOLIA=


# If you want to set your database path, you can specify it here (either direct path or relative path, in which case the working directory is the server.go (or server binary) parent directory
ME_DATABASE_PATH=./database/memeextra.db


# If there's already a deployed smart contract on Sepolia, you can specify it using ME_SMACON_ADDRESS
